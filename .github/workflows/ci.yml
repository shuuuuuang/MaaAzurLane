name: CI

on:
  workflow_dispatch:
  push:
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install test tools
        run: |
          python -m pip install --upgrade pip pytest ruff
          python -m pip install -e .

      - name: Lint
        run: ruff check .

      - name: Validate pipeline JSON
        run: python tools/validate_pipeline.py

      - name: Validate screenshot replay metadata
        run: python tools/replay_screenshot.py --metadata-only

      - name: Test
        run: pytest
